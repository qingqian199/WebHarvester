import { handleToggleFeatures } from "../toggle-features";import { FeatureFlags } from "../../../core/features";describe("handleToggleFeatures",()=>{beforeEach(()=>{jest.clearAllMocks();for(const key of Object.keys(FeatureFlags)){(FeatureFlags as any)[key]=true;};FeatureFlags.enableFullCaptureEnhanced=false;});it("�����ɷ���",()=>{expect(typeof handleToggleFeatures).toBe("function");});});